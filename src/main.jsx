import React from "react";
import { createRoot } from "react-dom/client";
import VictorianSmash from "./VictorianSmash.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VictorianSmash />
  </React.StrictMode>
);
